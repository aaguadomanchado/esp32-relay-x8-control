#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// Credenciales por defecto para el modo Access Point (AP)
// Edita estos valores antes de compilar
const char *DEFAULT_AP_SSID = "ESP32-Relay-X8";
const char *DEFAULT_AP_PASSWORD = "12345678";

// Credenciales por defecto para el modo Station (STA) - conexión a red WiFi
// Edita estos valores antes de compilar
const char *DEFAULT_STA_SSID = "MiFibra-C500";
const char *DEFAULT_STA_PASSWORD = "53409478m";

#endif // CREDENTIALS_H