#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// PLANTILLA — copia este archivo como include/credentials.h y rellena tus datos.
// credentials.h está en .gitignore y NUNCA debe subirse al repositorio.

// Credenciales por defecto para el modo Access Point (AP)
const char *DEFAULT_AP_SSID = "ESP32-Relay-X8";
const char *DEFAULT_AP_PASSWORD = "CAMBIAME-8CARACTERES-MIN";

// Credenciales por defecto para el modo Station (STA) - conexión a red WiFi
const char *DEFAULT_STA_SSID = "TU_WIFI";
const char *DEFAULT_STA_PASSWORD = "TU_CONTRASENA_WIFI";

#endif // CREDENTIALS_H
